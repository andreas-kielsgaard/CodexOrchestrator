# Views

Views are rendering-focused React components.

They receive view models, status values, and callbacks from controllers. Views should remain
boring: no persistence, no CLI/runtime calls, no domain orchestration, and no cross-flow state
coordination.
