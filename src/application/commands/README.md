# Commands

Canonical import home for state-changing application contracts and use cases.

These files currently re-export the existing root-level application modules so the
product work on `main` can move into the new architecture without losing the
agent-session and orchestration surfaces. The root modules can be turned into
compatibility shims after the remaining callers migrate.
