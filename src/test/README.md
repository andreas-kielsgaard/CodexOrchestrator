# Test

`src/test` owns shared test setup and test-only utilities.

Keep production helpers out of this folder. If a helper is useful at runtime,
move it to the folder that owns the corresponding production concept.
