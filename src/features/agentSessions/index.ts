/**
 * Agent Session feature boundary.
 *
 * Later slices compose the session controller and views here through the application boundary.
 * Runtime identity, durable history, raw CLI arguments, and process ownership stay outside the
 * feature layer.
 *
 * AS-00 intentionally exports no UI or behavior.
 */
export {};
