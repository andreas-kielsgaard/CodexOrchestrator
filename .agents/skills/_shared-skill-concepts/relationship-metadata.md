# Relationship Metadata

Relationship metadata is only a recovery map for actor ids, roles, parent or addressed-by links, return destinations, lifecycle state, and orchestration-home pointer.

Do not store task payloads, prompts, acceptance criteria, implementation details, worker reports, or orchestration history there.
