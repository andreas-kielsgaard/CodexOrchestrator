# Root Intake Wakeup

Intake refresh belongs to the root control role. Record maintenance may request a root-sourced intake when maintained state changes direction-relevant facts.

Reuse an active intake for the same update. If the required thread action is unavailable, emit the exact intake request and destination.
