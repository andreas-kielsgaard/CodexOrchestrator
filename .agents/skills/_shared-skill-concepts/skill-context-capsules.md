# Skill Context Capsules

A compact skill context capsule may orient a coordinating actor to relevant skill names, paths, current topology, return routes, and reload-after-compression needs.

Do not use a capsule as a stale copy of skill instructions. Lower execution roles should receive only the role-local skill and context they need.
