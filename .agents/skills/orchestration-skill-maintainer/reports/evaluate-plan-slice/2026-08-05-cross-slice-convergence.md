# Cross-slice convergence ownership

## Observation

Exact accepted Slice tips were merged successfully, then required corrections across ten files and a divergent race assertion. The issue was interaction among accepted privacy, recovery, and race semantics, not a textual conflict or evidence of overlapping worker ownership.

## Reader and cause

The Overall Plan conversation evaluates returned movement and decides what becomes ready next. It must recognize uncovered cross-slice composition risk, but it should not execute repository integration or absorb detailed implementation.

## Revision

Judge the new boundary created when a returned Slice combines with earlier accepted movement. When their interaction adds uncovered risk, hold dependent work behind a separately owned convergence Slice with its own acceptance boundary.

## Evaluation

This keeps cross-slice coherence visible without adding an integration role or skill. Standard Plan Slice ownership already provides planning, execution, review, and callback behavior, so a new operation would duplicate the catalogue and blur the Overall Plan boundary.
