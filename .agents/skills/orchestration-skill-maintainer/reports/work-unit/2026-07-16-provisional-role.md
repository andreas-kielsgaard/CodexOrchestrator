# Provisional Work Unit

Status: created. No product Work Unit action is claimed available.

## Trigger

The proposed first serial Sprint needs one coherent implementation responsibility to complete fully before Sprint judgment.

## Why adjacent skills are insufficient

`orchestration-worker` belongs to the older Work Slice delegation and merge pipeline. `work-unit-review` begins after implementation returns. Neither gives the implementation task a concise product-aligned boundary.

## Wording and expected effect

The skill owns one fixed outcome, local implementation judgment, proportionate validation, and truthful return. It proactively callbacks before ending, emits an explicit callback payload if messaging is unavailable, avoids routine progress traffic, and rejects partial-progress finalization and self-acceptance.
