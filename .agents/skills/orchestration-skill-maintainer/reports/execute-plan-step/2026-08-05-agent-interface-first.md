# Agent interface loading

## Observation and theory

The execution operation covered productive validation but did not trigger the dedicated interface skill before desktop-control choices.

## Revision and evaluation

The operation now applies `$agent-interface-first` for software interaction work that might use desktop automation. This affects mechanism selection without expanding the fixed Plan Step outcome.
