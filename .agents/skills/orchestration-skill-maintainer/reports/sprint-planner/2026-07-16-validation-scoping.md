# Provisional Sprint Planner

Status: revised for the current Epic flow. No product code changed.

## Triggers

In Sprint Runner session `019f669c-6251-70b2-9d96-8c6f2e4275ae`, WU-S5-14 began as one complete Work Unit. A later prompt kept its scope but prescribed targeted proof across named boundaries and deferred named integration checks.

Worker session `019f6acb-0122-7e02-9cc7-1840b37a85b3` then stopped with known implementation and local evidence unfinished. Recovery prompts selected exact test matrices and connector checks, further turning validation into parent-planned sub-assignments. The worker's repeated final progress reports were a separate completion failure that amplified the misunderstanding.

The latest Epic proposal in task `019f48bb-85b0-7451-bf2c-5483a36a18ff` asks each Sprint to add a usable product transition, run independently through internal gates, and preserve user control at consequential boundaries.

## Role

The provisional Sprint Planner owns one bounded Sprint from Epic handoff through accepted, partial, or blocked return. It defines Work Units, handles ordinary gates, reviews outcomes, and judges whether the intended Sprint movement was achieved.

The older `sprint-runner` remains the comprehensive development procedure. This skill is a smaller bridge aligned with the emerging product role and current ad-hoc task flow.

## Revision theory

The earlier narrow skill stopped at plan handoff, while the current flow needs the same Sprint owner to preserve intent through launch, review, correction, and Sprint judgment. Carrying the whole `sprint-runner` procedure forward would also preserve too much launch-register and model-routing machinery.

## Revision concept and expected effect

Keep one concise role covering plan, provisional launch, independent review, and Sprint closeout. Preserve the validation clue at the Work Unit handoff:

> If useful, give a broad clue about where validation belongs in the sequence. Leave tests, checks, commands, and method to the Work Unit. A later validation point does not move implementation, deliverables, or acceptance out of the Work Unit.

Name a later owner when validation is deferred. Do not add upstream role topology to a downstream skill.

The revision should allow the current ad-hoc Sprint task to run with less supervision while keeping product actions truthful and validation design downstream. Each Work Unit receives one launch and an exact proactive callback route; the Sprint Planner waits rather than polling and reads child detail only when the final report is insufficient. It remains provisional until live product-role behavior exists.
