# Agent-interface-first loading

## Observation

Work Unit reviewers independently select evidence and may otherwise default to a visible walkthrough for UI work.

## Revision

Added one loading boundary before result review: load `$agent-interface-first` before choosing visible UI control as evidence.

## Evaluation

The hook preserves reviewer judgment while ensuring the shared interface principle is available before evidence selection.
