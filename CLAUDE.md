# Agent notes

- Development and validation commands: [docs/development.md](docs/development.md).
- In a Linux cloud session, read [Linux cloud sessions](docs/development.md#linux-cloud-sessions) before running the Rust suite. It covers the extra setup and lists the Rust tests that are known to fail on Linux, so they are not mistaken for regressions.
