# Validation evidence

This is a dated account of the evidence that changed the project's understanding, not a claim that all these checks pass on every later checkout. The documentation rewrite researched source at `e2bfc6c`, recovered selected original messages from 41 tasks, and reconciled the guides with concurrent main changes through `60c3798`. It did not rerun application builds, migrations, authenticated providers or a native UX audit.

Current behavior and rationale live in the subject guides. Recorded clients, tests, native process activity, durable records, user acceptance and publication establish different facts. A later successful checkpoint can supersede an earlier limitation without proving why every earlier failure occurred.

## Agent Session recovery and app-server integration

The July recovery separated durable Session context from its visible conversation, replaced the old process manager with supervised process ownership, and isolated provider parsing from presentation. AS-02/03/05/06 producer/review messages and the foundation task establish those boundaries; AS-04's original substantive turn was unavailable. Source: foundation `019f48bb-85b0-7451-bf2c-5483a36a18ff`, raw lines 424, 645, 823; historical accounts at `e2bfc6c:docs/agent-session/evidence.md` and `e2bfc6c:docs/agent-session/execution-ledger.md`.

The old implementation's failures concerned competing stores/identities, fabricated or missing history, process lifecycle versus UI state, contract drift and misleading verification. The corrective architecture puts records, process ownership, native adaptation and projection in separate owners. The detailed original failure list remains retrievable from that evidence file; it is not a list of unresolved current bugs.

| Checkpoint                                      | Evidence retained                                                                                                                                                                                                                                                                                                                                                   | Limit or later change                                                                                                                                                                                                                                                    |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| July 12 recovery                                | Non-live checks reported 339 frontend and 84 Rust tests, with two intentional ignores; recorded responsive checks covered three widths. A native retry observed working/streaming, a durable failed terminal state and restart/reopen history.                                                                                                                      | Codex hit a usage limit before a provider thread was created. That run did not prove successful completion, provider continuation, concurrency or cancellation. The later app-server implementation supersedes its runtime description.                                  |
| Main-based Session port, `e914a9e`, September 9 | Reconstructed on `deecb2f`, excluding OTP ancestry. All eight failures in an initial 698/706 native sweep were addressed in the final 328-test focused run; a separate Session run passed 73 tests. Frontend coverage accounted for 961 tests, with the excluded OTP-picker fixture corrected and its four-test file passing.                                       | These are the reported sweep/focused scopes, not one invented all-green full-suite rerun.                                                                                                                                                                                |
| Installed executable contract                   | Native Codex CLI 0.144.0 passed the local Responses-fixture contract for steering, interruption, resume and inherited defaults; 40 inspector tests passed.                                                                                                                                                                                                          | Installed CLI/local provider fixture, not real-model quality or authenticated-provider acceptance.                                                                                                                                                                       |
| Native disposable-environment passes            | Required default selection, empty retained workspace, approval and invalid-response recovery, two steering inputs/exact retries, refreshed defaults, sandbox mismatch/recovery and restart persistence were exercised through owned native command handles. A fifth invocation after restart retained the original thread/workspace and ready Workflow MCP binding. | Disposable native homes and a local provider fixture. Wide/narrow captures were inspected, but keyboard/accessibility acceptance was not established. A collector's expectation of a provider `tools` field was corrected using persisted protocol/MCP-startup evidence. |
| Main integration/publication                    | The curated Session port and cleanup were incorporated and published through `ac22f01`; `e914a9e` is an ancestor of the documentation baseline.                                                                                                                                                                                                                     | The original port document's final “no merge/push” sentence described its own earlier stage. Publication is a later separate fact.                                                                                                                                       |

Earlier convergence records establish narrower stages. Sprint 3 reported 527 frontend and 98 Rust tests (two ignored), plus an installed version/help probe without a live prompt. Process ownership was direct-child-only then, and a recorded-harness timing regression remained unresolved despite the documented rerun passing. Sprint 4 reported the recorded first-message, acknowledged-resume, title and proposal boundary with 535 frontend tests, no Rust rerun and no live-provider or durable-proposal proof. Their document-reported acceptance is distinct from independently recovered human acceptance. Sources: `e2bfc6c:docs/agent-session/sprint-3-convergence-evidence.md`, lines 64–125, and `e2bfc6c:docs/agent-session/sprint-4-convergence-evidence.md`, lines 5–41.

Orientation Discovery recorded G2 acceptance of a shared conversation/excerpt surface and contained overview/detail direction, with 363 frontend and 87 Rust tests (two ignored). Auto-flow represented nonexecuting local intent, the Epoch dialog was a placeholder and the types were provisional. That recorded stage did not establish production persistence or runtime integration; its default-navigation and future-Epoch instructions are superseded. Source: `e2bfc6c:docs/agent-session/orientation-discovery-final-handoff.md`, lines 7–75.

The fresh native pass exposed missing production initialization of existing Sprint/Bootstrap storage that test openers had supplied. The corrected central assembly and schema-48 upgrade/reopen tests are implementation evidence; they are not a license to infer every migration path passed.

Source: task `01a0815c-71b3-7423-857e-07009d705763`, original integration and completion messages; `e2bfc6c:docs/agent-session/session-main-integration.md`. Local `.dev/session-main-port-20260909` records were supporting artifacts, not portable prerequisites for reading this account. See [Sessions](agent-session/README.md) and [configuration](execution-configuration.md).

## Session Event regressions and repair

At `4bded63`, the September 7 review reproduced nine browser issues while the existing frontend suite still passed 980 tests. The browser used actual React/CSS with fake clients. Backend findings were source traces, not a live native run. This demonstrates the gap between isolated/old-route tests and the mounted happy flow.

The affected contracts were ordinary first/second sends, instance/worktree persistence, completion-to-delivery routing, pinned configuration after shared edits, and editor draft/activation ownership. Layout findings included the lost graph, hidden narrow-screen controls, oversized checkboxes and collapsed sections remaining visible. Accepted changes such as role removal and per-message choices were explicitly not classified as regressions.

| Stage                                    | Recorded evidence                                                                                                                                                                            | Interpretation                                                                                                                                                                         |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Repair on base `e77a725`, then `c1b89b9` | 997 frontend tests, nine joined Rust repair tests, TypeScript/Vite and native build; four-width recorded-browser checks.                                                                     | Joined tests used real repositories/services/local HTTP proxy with a recording inference runtime. They exercised normal terminal notifications and SQLite reopen, not a paid provider. |
| Full native repair sweep                 | 711 passed, one existing orchestration MCP timeout, two ignored. The timed-out `work_slice_planning_request_launches_one_prepared_planner_and_marks_readiness` passed in isolation.          | The isolated pass does not erase the full-suite timeout.                                                                                                                               |
| Harness UX integration and follow-up     | The repair was integrated into the Harness UX branch, with 997 frontend/nine joined Rust/build checks repeated. Later `2e378bc` UI work and `deecb2f` integration preceded the Session port. | Browser screenshots and integration ancestry do not become native keyboard, restart or provider acceptance by themselves.                                                              |
| Walkthrough correction                   | The old merged `10-saved-instance.png` shows list-style instances; the repair `05-saved-instance.png` shows the restored graph.                                                              | Those two images were inspected in the baseline. The decks and all other screenshots were not freshly reviewed; the old v7 README's “current” wording was misleading.                  |

The old repair's synthetic default-profile recovery is superseded by the later explicit saved-default selection requirement. Preserve the protected first/second-send and pinning behavior, not the obsolete default-creation policy. [Regression assets and replay](regression-review/README.md) and the [walkthrough index](ux/session-event-model-walkthrough/README.md) keep baseline and repair evidence distinct.

Source: `e2bfc6c:docs/regression-review/README.md`, `repairs/verification.md` and `repairs/pre-merge-audit-2026-09-08.md` under the same directory; original tasks `01a0390e-64bb-7b01-b277-e019f37d51bc` and `01a07aa8-4bdb-7ba1-923c-cd444ad477fc`, with the later Session integration task above.

## Epic/Sprint proof boundaries

The original first-run Task dashboard manual gate found that clean databases lacked repository/worktree setup despite passing component checks. Worker 051 supplied registration. This is a historical lesson about exercising a usable first-run path, not a current main defect. Source: `e2bfc6c:docs/orchestration-log.md` and `e2bfc6c:docs/task-logs/worker-051-repo-worktree-setup-path.md`.

### Sprint 5 G3 correction

The preserved G3 sample records a successful server-confirmed save through the old two-tool flow. Its `item_5` completed with `error: null`, `status: persisted`, revision `proposal-revision-85cb81b4-6824-4801-8dd8-26e1934feccb`, provenance and event identities. The final response repeated that revision. Started/completed records reuse item identities and do not double the number of tool calls.

WU-S5-12 task `019f6a4e-732e-7532-ae03-921ef2d9c567`, raw line 9, identifies the original attachment `3efa326d-9652-4efa-9593-e35176092852/pasted-text.txt`; lines 175–190 and 230 carry the result. This corrects the blanket failed-save statement at `e2bfc6c:docs/orchestration/sprint-5-convergence-evidence.md:96`, while preserving the narrower account at lines 109–112.

The transcript is not an independent database reopen/read-query check and does not prove the corrected one-call contract, complete flow or user acceptance. The original human G3 review turn was not recovered. A separate diagnostic task, `019f6b7a-0808-79c3-b840-cf7c7c1ca969`, reported a different failing concurrency path involving SQLite event/proposal writers. That historical diagnosis does not negate the successful sample.

`design-qa.md` separately reported that the Sprint 5 layout had no implementation screenshot available. Its DOM tests did not establish visual fidelity. No later exact-change visual acceptance is invented to close that record. Immutable source: `e2bfc6c:design-qa.md`.

### Sprint 6 and later execution stages

The former 1,036-line Sprint 6 record mixed an early partial heading with later implementation. The useful evidence is its sequence of separately established boundaries:

| Stage                                             | What the historical record establishes                                                                                        | Immutable source range                                                                                   |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Plan Builder, confirmation and Bootstrap          | Harness/provenance persistence and isolated provider proof; human confirmation at the original checkpoint remained open.      | `e2bfc6c:docs/orchestration/sprint-6-convergence-evidence.md`, lines 140–191; WU-S6-01/02/03 companions. |
| Sprint activation/start/reevaluation              | Correlated pre-start result, explicit Epic start authorization, same-Sprint reevaluation.                                     | Lines 206–230.                                                                                           |
| Work Slice planning and Work Unit materialization | Planner request versus accepted material; immutable selected revision owns the executable graph.                              | Lines 256–314.                                                                                           |
| Handler/Implementer sequence                      | Initial Handler, exact Implementer attempt/specification, reporting continuation, file-review readiness and Handler judgment. | Lines 344–632.                                                                                           |
| Accepted integration and dependent work           | Correlated candidate, Git integration, durable contribution, dependency-wave eligibility and settlement.                      | Lines 688–738.                                                                                           |
| Completion and upward settlement                  | Graph drain, planning-point settlement, Sprint continuation/upward result and Epic reassessment remain distinct outcomes.     | Lines 822–947.                                                                                           |

WU-S6-01 first recorded a proposal-only live probe ending in `internal_error` without a save, then an immediate rerun with one durable proposal. Neither requested initiation, so later confirmation fixes do not explain the first failure. WU-S6-02 separately established bounded fresh Bootstrap attempts after startup interruption and gating to the same attempt; it did not establish process reattachment or race-proof containment. Sources: `e2bfc6c:docs/orchestration/sprint-6-wu-s6-01-evidence.md`, lines 31–42, and `e2bfc6c:docs/orchestration/sprint-6-wu-s6-02-evidence.md`, lines 24–48.

These are historical deterministic or controlled-live reports, with some broader-suite failures/timeouts and unproven downstream boundaries. Present source contains the runtime families; that does not establish a newly accepted end-to-end Epic/Sprint flow. The later user reduction in Epic/Sprint priority also does not delete this implementation evidence.

Provenance correction: user-authored discussion is User source; fixed product Plan/Rebuild prompts are Application source. Button context is launch-only, and restart correlation uses persisted invocation provenance. Routed original evidence: task `019f6bd0-7db6-7931-ab21-66a2e3c70232`, raw line 174. The role/material/acceptance meaning lives in the [Epic/Sprint guide](orchestration/README.md).

## Product Decisions and File Review

The recorded Product Decisions exploration was corrected toward a separate view, a simple list, hierarchy only from typed relationships and exact passage navigation. Broad pane-position policy was rejected. Routed source: `019fbe99-1f06-7433-ae70-73a805bd9a01`, raw lines 71/80; recorded implementation and reviewer outcomes at 171/215 are sender reports, not a fresh live audit.

The later authority assignment required immutable official/current accepted versions, agent output remaining proposal-only, and applied/published state staying separate. Source: `019fd739-8a4f-7ce3-88cd-5f85ee6ff468`, raw line 20. The independent audit's final outcome and owner task `019fd64e-fa7a-76c1-8a41-1797b62200d5` were not recovered. Current persistence/correction source supports the implemented semantics; it does not supply the missing audit verdict.

Exact supporting-passage navigation and Back restoration at `f51ecd4` were accepted in a narrow independent recorded-route audit (`019fd625-747c-7643-b896-686abbe709b6`, raw line 316). Unrelated Harness failures and no live/product acceptance remained explicit. See [Product Decisions](product-decisions.md).

For File Review, the original read-only viewer acceptance (`019f6e52-184d-7573-af77-479f9883223f`, raw line 778) left native integration and the empty-source correction for later work. Continuation `019f6ed6-ee68-79d1-b88e-80505d13a163` reported `ba130cf`, followed by `d4eb0de` scoped sources and `7bd5545` Sprint review integration. These establish implemented layers, not completed native startup composition.

A closer source trace during the documentation rewrite corrected the research baseline: at `e2bfc6c`, `src-tauri/src/active_app.rs` installs `ContextualFileReviewTauriState::unavailable`; `orchestration/transport.rs` returns `not_ready` when its producer service is absent. The scoped loader, frontend/native client, commands and producer implementation exist, but a fresh Sprint-context review cannot be produced through ordinary product startup. This remaining connection is documented in [File Review](file-review.md), without changing code in this task.

## Database and repository integration

The managed ActiveDatabase decision followed the concurrency failure and direct parallel-work requirement in task `01a03977-7ea1-7aa0-9db4-1728cb02dd06`. Demonstrated-scope acceptance and integration/publication are separate messages; they do not mean every cross-process stress scenario was exercised. Shared repository registration and live Git discovery retain separate authority; [ActiveDatabase](architecture/active-database.md) and [Worktree Review](worktree-review.md) own those contracts.

The September 14 Worktree Review integration has later, stronger evidence than the September 9 plans:

| Stage                                   | Evidence                                                                                                                                                                                                          |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Feature/regression checkpoint `607085c` | 696 native and 964 frontend tests on rerun; native selection, paging, keyboard-related flows and cancellation observations.                                                                                       |
| Combined local integration `057c4c7`    | 719 native and 972 frontend tests, release build, migration, actual checkout/build/Open/restart and a Windows long-path correction.                                                                               |
| Retained operational database migration | Version 6→7 with unchanged hashes for all 14 data tables, plus fresh native startup/registration and output/focus reuse in the isolated integration scenario. This is not the active-product schema-48 migration. |
| Publication `e2bfc6c`                   | The user ended refinement and requested publication after the local integration result; main was pushed.                                                                                                          |

Source: task `01a0853c-31a2-77e3-81af-4aa1b086ff7d`, raw lines 3682, 3762, 4580, 4587 and 4683; `e2bfc6c:docs/orchestration/worktree-review-main-integration-report.md`. The retained local report `.dev/worktree-review-completed-20260914/integration/report.md` was read during baseline research. Old `.dev/branch-navigation` links into a retired checkout were not treated as current paths.

Remaining observations include overlapping ancestry-range counts, scrolling/tooltip coverage, an intermittent frontend test and ancillary inspector issues. The isolated migration/startup/build/open result is not all-platform, installer, physical-keyboard or screen-reader acceptance. Forked review/merge conversations repeat portions of the same history and are not independent corroboration.

## Native-home evidence

NCHP-3 on August 6 (`6943d5d`) was a partial isolated checkpoint: debug/release builds, owned launch/reopen, an NSIS artifact that was not installed, and native-profile/frontend checks. Setup facts, command acceptance, process observation, readiness and actual provider capability were separate. Its defining failed sandbox probe used actual CLI `0.130.0-alpha.5`, while the earlier account had recorded `0.144.0`; the command omitted required `--sandbox-state-json` and exited before UAC or canary proof. This is a dated incompatibility, not a finding about the current CLI.

The later audit closure at frozen `6fd276f` (tree-equivalent chain `2ba9bed`) accepted bounded Workspace Write and Danger/Full Access canary, receipt and cleanup results, plus cold-reopen evidence. It also retained residuals: unobserved browser handoff, externally elevated sandbox adoption versus unobserved product UAC, retained workspace-policy attention, unassessed MCP/reporting and unexercised downstream semantics.

The original NCHP-3 producing attempt was not recovered; the surviving record and subsequent audit were reviewed. Sources: `e2bfc6c:docs/orchestration/nchp-3-controlled-live-evidence.md` and `e2bfc6c:docs/orchestration/nchp-audit-readiness-closure.md`. Later Session integration supersedes using those old audit gates as the ordinary Session-binding procedure. Current [configuration](execution-configuration.md) separates selected native identity continuity from readiness.

## Legacy retirement and authenticated Session follow-up

Separate cleanup landed while this rewrite was running: `67b325a` removed disconnected Workflow UI and automatic status-server startup; `ac18781` removed the original Task stack and ten native command registrations. The active app-server, Session lifecycle, active-v3 storage and repository/Worktree Review services remained. These changes are inherited from main, not implemented by the documentation task.

The separately owned [retirement record](architecture/legacy-task-retirement-plan.md#execution-record) reports 681 frontend tests in 122 files and coverage of all 695 native tests: 278 retained serial results plus 417 passing batched results, not one invented complete rerun. Rust check, the capability-boundary integration test, frontend and desktop builds passed. Source lint and changed-file formatting passed; full-project lint and full Rust formatting retained pre-existing findings outside that change. Disposable desktop startup, seeded Session reopen/restart and unchanged older-database sentinels established their named storage/UI boundaries.

The subsequent [real-agent verification](architecture/legacy-task-retirement-verification.md), recorded at `60c3798` against `ac18781`, used authenticated Codex app-server with Sol. Five invocations retained one provider thread while exercising file-tool reading, a UI follow-up, cancellation and continuation. Initial and recovery submissions used native commands through the owned WebView; follow-up and cancellation used the composer/buttons. The recovery reply required Refresh because its out-of-band invocation was not loaded by the open controller.

Cancellation reached the persisted canceled state after 19.599 seconds; its delay remained unexplained. The earlier shell-wait scenario was rejected by policy, so the successful cancellation scenario used a long text response. These observations do not prove interruption of a running shell tool, all provider features or approval flows. This is the other task's reported executed evidence, not a fresh run during documentation consolidation. Both new source reports remain intact under their existing ownership.

## Developer tooling

The integrated tooling cleanup removed the recorded Session simulator, retained focused DTO-based tests, standardized npm setup and clarified validation commands. The [cleanup validation record](cleanup/tooling-build-cleanup-validation.md) records the original branch's frontend, Rust, release-build and native-launch checks separately from the integration checks. Those original native checks predate the later legacy retirement on main.

The retained helpers came from scoped developer work, not a policy to route every build or agent workflow through them. Reported reduced-debug artifacts had much smaller PDBs; cold timing order and machine caches prevented a controlled speed comparison. A fresh helper compile completed, and fake-only tests checked argument ownership, environment restoration and Cargo exit propagation.

The sccache measurements also include a useful negative result:

| Historical August 4 measurement                      | Result and limit                                                                                                                                                                                                                      |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Controlled cold versus warm cross-path `cargo check` | 414.090 s versus 351.619 s; warm result had 269 hits/106 misses (71.73% cacheable-request hit rate). It remained slower than an earlier 143.128 s no-wrapper baseline under different contention.                                     |
| Two simultaneous independent targets                 | Both Cargo builds finished in roughly 212/210 s; pair wall time 214.979 s. Shared counter deltas belonged to the overlapping pair. Numeric child exit codes were not retained; Cargo outputs/timing artifacts established completion. |
| Representative `cargo test --lib --no-run`           | No wrapper: 191.127 s; cache helper: 257.627 s, 34.79% slower. The prior check cache did not match the new code-generation/link shape. No favorable third run was added.                                                              |
| Local edit/recheck                                   | Ordinary incremental Cargo was faster in the measured edit. The helper's incremental disablement is process-scoped, not a new global default.                                                                                         |

These observations explain separate target ownership and opt-in caching; they are not current disk-size or universal speed claims. Warm cross-worktree no-run reuse remained unmeasured. Sources: `e2bfc6c:docs/orchestration/ad-hoc-rust-compilation-cache.md` and `e2bfc6c:docs/orchestration/rust-test-developer-validation.md`; tasks `019fcbd6-6e6a-74a1-9514-dad527bb9e36`, `019fcc24-b0dd-7240-a423-f435cc54a1af`, and `019fe0c7-2bda-7ee2-9c08-47b32fa8820e`.

## Provenance limits and separate work

The baseline read 311 text inputs fully and reviewed the long orchestration log structurally through all 115 sections, with its extracted decision/correction blocks. It inventoried associated assets and inspected two comparison screenshots, not every deck/image. Most producing conversations were read selectively; delegated instructions and assistant results are not silently relabeled as original human decisions.

Specific missing histories include AS-04, the original inspector request, initial NCHP-3 and capability-matrix production, portions of the early Harness/navigation explorations and the Product Decision audit closeout. Their surviving documents/current source can support qualified accounts; the missing conversations are not passed reviews. No decision is inferred from the offline package's blank worksheets.

Navigation `99912ce`, remote development `ab220ff` and OTP `8ef084b` were outside main ancestry when the rewrite began. Their branch outcomes and reported tests do not prove those features exist in this checkpoint. The [documentation index](README.md#work-outside-this-checkpoint) identifies that work without absorbing or deleting its active plans.

Use the [cleanup map](cleanup/documentation-rewrite-map.tsv) to locate the immutable original path/revision and its maintained destination. The source references above explain the claims without requiring the whole development conversation as onboarding material.
