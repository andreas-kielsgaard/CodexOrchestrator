# Documentation consolidation record

Status: completed on 2026-09-14 in `cleanup/documentation-consolidation`, based on main `60c3798`.

The rewrite implements the agreed subject-based consolidation. Research used source `e2bfc6cb584a9ce7ada0d762f768c605a33b4160`, 312 text inputs and selected original messages from 41 producing tasks. The normal reading path is now [project README](../../README.md) → [documentation index](../README.md) → the relevant subject guide.

## Result and disposition

Eighteen maintained guides and evidence indexes now explain current behavior, source ownership, decisions and dated proof. Seven existing files were rewritten, ActiveDatabase was reconciled in place, and ten new guides/indexes were created. The [312-row file map](documentation-rewrite-map.tsv) records each source's outcome and actual destination headings.

The rewrite removed 290 superseded documents after incorporation review. Within that set, 95 files primarily carried retired operational protocols; their relevant catalogue-retirement rationale is preserved without recreating those instructions. Useful material from the other sources was synthesized by subject. Immutable Git references preserve exact historical contents, including the 182-path saved-checkout reconciliation.

The redundant research reports, inventories and agent ledgers were removed from this worktree after their unique findings and provenance were incorporated. No archive tree was added. Original-checkout research and unrelated dirty work remain under their existing ownership.

## Evidence corrections

- The recovered Sprint 5 G3 attachment records a server-confirmed save for that sample. It does not establish independent database reopen, corrected one-call behavior or whole-flow acceptance.
- File Review's client, scoped loader, commands and producer implementation exist, but ordinary native startup installs an unavailable originating service. A fresh Sprint-context request returns `not_ready`.
- Later Worktree Review integration and native-home canary/reopen evidence supersede several earlier blanket proof gaps. The failed probes, unobserved boundaries and incomplete Product Decision audit history remain explicit.
- Historical test and visual evidence is dated. Recorded routes, local provider fixtures, authenticated provider work and user acceptance retain separate meanings.

See [validation evidence](../validation-evidence.md) for the source references and qualifications.

## Preserved ownership

Ten active instruction files and four active plans were excluded from the rewrite. The navigation plan remains unchanged relative to this branch's base. Its dirty original-checkout version, the two untracked remote plans and the untracked tooling plan were not imported or overwritten. Their map hashes identify the original research snapshot, not newly committed versions.

Generated/binary assets, build outputs, caches and worktrees were not removed. The three retained evidence packages have dated asset indexes. The only executable-code references to removed documents are recorded File Review display labels with inline fixture content, not filesystem dependencies.

The separate legacy/disconnected cleanup landed during this work. The branch was reconciled with main `60c3798`; current Task-retirement, launcher and status-tooling passages reflect that source. The two new legacy retirement/verification documents remain unchanged outside the original 312-input research corpus. Their relevant later proof is linked from the evidence guide. The removed prototype procedure's new historical-status note was incorporated into ActiveDatabase.

## Validation and integration

The source-to-destination audit checked every map row before deletion, including targeted original documents and current source where a claim was disputed. All 290 removed files matched both the research hashes and original Git content before removal.

Final checks passed after reconciliation:

- All 312 map outcomes are closed; all 712 destination references and 309 immutable tracked-source references resolve.
- All 350 local links/anchors across the 19 authored documents and two preserved legacy reports resolve; 50 inline immutable Git references resolve.
- Prettier checks pass for the 19 authored Markdown files, and `git diff --check` passes.
- All 80 retained non-Markdown package assets, ten active instruction files, the tracked navigation plan and both inherited legacy reports are unchanged relative to `60c3798`. There are no application-code or configuration changes in this branch's diff.
- Navigation, remote-development and OTP checkpoints remain outside the final base's ancestry.

Application builds, provider runs and native UX checks were not rerun for this documentation-only change. The historical and inherited evidence is attributed in the evidence guide.
