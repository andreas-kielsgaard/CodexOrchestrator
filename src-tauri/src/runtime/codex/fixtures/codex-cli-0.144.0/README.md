# Codex CLI 0.144.0 JSONL fixtures

These checked-in protocol recordings use the event shapes already captured by the repository's
Codex parser evidence and are labelled against the locally installed `codex-cli 0.144.0` help
surface verified on 2026-07-10. The resume, malformed/unknown, and byte-chunk files are deterministic
variants of that recording for adapter tests. No live or paid agent prompt was run to create them.
